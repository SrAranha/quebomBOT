import { HelixBanEvent, HelixResponse } from 'twitch';
import { HelixBanEventData } from 'twitch/lib/API/Helix/Moderation/HelixBanEvent';
import WebHookListener from '../WebHookListener';
import Subscription from './Subscription';
/**
 * @private
 */
export default class BanEventSubscription extends Subscription<HelixBanEvent> {
    private readonly _broadcasterId;
    private readonly _userId?;
    constructor(_broadcasterId: string, handler: (data: HelixBanEvent) => void, client: WebHookListener, _userId?: string | undefined, validityInSeconds?: number);
    get id(): string;
    protected transformData(response: HelixResponse<HelixBanEventData>): HelixBanEvent;
    protected _subscribe(): Promise<void>;
    protected _unsubscribe(): Promise<void>;
}
